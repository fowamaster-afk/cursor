/**
 * services/
 * --------
 * Lapisan layanan (API client, koneksi database, fetching data eksternal).
 *
 * Sesuai lingkup saat ini, belum ada logika database atau permintaan
 * jaringan. Isi modul ini nanti dengan fungsi-fungsi layanan terpisah
 * (mis. `auth.ts`, `session.ts`) yang dipanggil lam oleh komponen/hook.
 */
export {};
